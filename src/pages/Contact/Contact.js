export const Contact = (props) => {
    return (
        <div className="contact">
            <form>
                <input placeholder="Nome" />
                <input placeholder="Email" />
                <textarea></textarea>
            </form>
        </div>
    );
}