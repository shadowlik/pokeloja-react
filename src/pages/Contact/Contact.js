export const Contact = (props) => {
    return (
        <div className="container">
            <form>
                <input placeholder="Nome" />
                <input placeholder="Email" />
                <textarea></textarea>
            </form>
        </div>
    );
}